{
	"build": {
		"dockerfile": "Dockerfile",
		"context": ".."
	},
	"appPort": [
		"9123:8123"
	],
	"mounts": [
		"source=${localWorkspaceFolder}/../pyisyox,target=/workspaces/pyisyox,type=bind,consistency=cached"
	],
	"postCreateCommand": "bash .devcontainer/scripts/setup_ha_test_env.sh || (for req in $(jq -c -r '.requirements | .[]' custom_components/isy994/manifest.json); do python3 -m pip install $req; done); if [ ! -d \".homeassistant\" ] || [ ! -f \".homeassistant/configuration.yaml\" ]; then bash .devcontainer/scripts/setup_homeassistant_runtime.sh; else echo '.homeassistant exists, skipping runtime setup'; fi",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"github.vscode-pull-request-github",
				"ms-python.vscode-pylance",
				"ryanluker.vscode-coverage-gutters"
			],
			"settings": {
				"files.eol": "\n",
				"editor.tabSize": 4,
				"python.pythonPath": "/usr/bin/python3",
				"python.linting.pylintEnabled": true,
				"python.linting.enabled": true,
				"python.formatting.provider": "black",
				"editor.formatOnPaste": false,
				"editor.formatOnSave": true,
				"editor.formatOnType": true,
				"files.trimTrailingWhitespace": true
			}
		}
	}
}
